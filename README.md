# PlanFit Web

Interface React/Vite do PlanFit.

## Arranque rápido

1. Copie `.env.example` para `.env`.
2. Execute `npm install`.
3. Execute `npm run dev`.

Por omissão, o frontend abre em `http://localhost:5173` e liga-se à API em `http://localhost:5000/api`.
